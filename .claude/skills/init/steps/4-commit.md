## STEP 4: コミット

git が未初期化の場合は `git init` を先に実行する。

以下の順でコミットする:

1. `.gitignore`（存在する場合）
2. `docs/adr/.gitkeep`
3. `docs/cc-knowhow.md`
4. `docs/dev-methodology.md`
5. `CLAUDE.md`

@../../git-commit/SKILL.md
