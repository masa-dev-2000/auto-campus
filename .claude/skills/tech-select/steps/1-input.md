## STEP 1: 選定対象の確認

`$ARGUMENTS` が指定されている場合はカテゴリとして使用する。
空の場合は以下を聞く:

```
どの技術カテゴリを選定しますか？
（例: データベース、認証ライブラリ、STT プロバイダー）

候補が既に絞られている場合は教えてください。
```

続けて以下を確認（一度にまとめて）:

```
1. このシステムでその技術が担う役割は？（1文で）
2. 重視する条件は？（速度 / コスト / 学習コスト / 実績 から上位3つ）
3. 除外条件はありますか？
```

@../../confirm-gate/SKILL.md
